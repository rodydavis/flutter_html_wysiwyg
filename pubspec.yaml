name: html_wiswyg
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  csslib: ^0.16.1
  html: ^0.14.0+3
  sanitize_html: ^1.3.0
  provider: ^4.0.5
  html_builder: ^1.0.5
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/samples/
